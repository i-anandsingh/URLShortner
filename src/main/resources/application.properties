spring.application.name=urlshortner
server.port=8081

# MySQL Configuration

spring.datasource.url=jdbc:mysql://localhost:3306/urlshortner

spring.datasource.username=i.anandsingh

spring.datasource.password=304506

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# MySQL Dialect (optional in newer Spring Boot versions)

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect